{
  "name": "chargepoint",
  "version": "1.0.0",
  "description": "Function to check if charge point next to the house is occupied",
  "main": "gulpfile.js",
  "scripts": {
    "build": "yarn run build:functions",
    "dev": "yarn run serve:functions",
    "build:functions": "netlify-lambda build src/lambda",
    "serve:functions": "netlify-lambda serve src/lambda"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "netlify-lambda": "^0.2.0",
    "run-sequence": "^2.2.1"
  },
  "dependencies": {
    "axios": "^0.21.0"
  }
}
